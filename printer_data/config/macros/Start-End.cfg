[gcode_macro _PRINT_TRACKER]
description: Tracks printing state for toolchanger
variable_is_printing: False
gcode:

[gcode_macro _SLICER_TOOL_PARAMS]
variable_retract: {'0': {'length': 0.5, 'speed': 31, 'unretract': 31}, '1': {'length': 0.6, 'speed': 30, 'unretract': 30},  '2': {'length': 0.6, 'speed': 30, 'unretract': 30},'3': {'length': 0.6, 'speed': 30, 'unretract': 30}}
gcode:

[gcode_macro _SLICER_TOOL_PA]
variable_pa: {'0': 0.02, '1': 0.02, '2': 0.02, '3': 0.02}
gcode:





[gcode_macro PRINT_START]
description: Toolchanger print start - compliant with KTC-Easy and Cartographer touch docs
variable_printing: False
gcode:

  {% set TOOL = params.TOOL | default(-1) | int %}
  {% set TOOL_TEMP = params.TOOL_TEMP | default(0) | int %}
  {% set BED_TEMP = params.BED_TEMP | default(60) | int %}
  {% set FILAMENT = params.FILAMENT | default("PLA") | upper %}
  {% set DEBUG = params.DEBUG | default(0) | int %}

  # FIX: safe reset (no crash if macro missing keys)
  {% set empty_dict = {} %}
  SET_GCODE_VARIABLE MACRO=SAVE_TOOL_TEMPS VARIABLE=saved_temps VALUE="{empty_dict}"
  SET_GCODE_VARIABLE MACRO=SAVE_TOOL_TEMPS VARIABLE=saved_active_tool VALUE=-1

  # FIX: guard toolchanger existence
  {% set retract_map = {} %}
  {% if printer.toolchanger is defined %}
    {% for tool_num in printer.toolchanger.tool_numbers %}
      {% set rl = 'T' ~ tool_num|string ~ '_RETRACT_LEN' %}
      {% set rs = 'T' ~ tool_num|string ~ '_RETRACT_SPEED' %}
      {% set us = 'T' ~ tool_num|string ~ '_UNRETRACT_SPEED' %}
      {% set tool_obj = printer['tool T' ~ tool_num|string] %}
      {% set r_len = params[rl] | default(tool_obj.params_retract_length, true) | float %}
      {% set r_spd = params[rs] | default(tool_obj.params_retract_speed, true) | float %}
      {% set u_spd = params[us] | default(tool_obj.params_unretract_speed, true) | float %}
      {% set _ = retract_map.update({tool_num|string: {'length': r_len, 'speed': r_spd, 'unretract': u_spd}}) %}
    {% endfor %}
  {% endif %}

  SET_GCODE_VARIABLE MACRO=_SLICER_TOOL_PARAMS VARIABLE=retract VALUE="{retract_map}"


  # FIX: PA map guard + safer default handling
  {% set pa_map = {} %}
  {% if printer.toolchanger is defined %}
    {% for tool_num in printer.toolchanger.tool_numbers %}
      {% set pa_param = 'T' ~ tool_num|string ~ '_PA' %}
      {% set tool_obj = printer['tool T' ~ tool_num|string] %}
      {% set extruder_name = tool_obj.extruder %}
      {% set pa_default = printer[extruder_name].pressure_advance %}
      {% set pa_val = params[pa_param] | default(pa_default, true) | float %}
      {% set _ = pa_map.update({tool_num|string: pa_val}) %}
    {% endfor %}
  {% endif %}

  SET_GCODE_VARIABLE MACRO=_SLICER_TOOL_PA VARIABLE=pa VALUE="{pa_map}"



  # ==========================
  # FRIENDLY START REPORT
  # ==========================

  RESPOND MSG="========== PRINT START =========="
  RESPOND MSG="Bed Temperature : {BED_TEMP}C"
  RESPOND MSG="Starting Tool   : T{TOOL}"
  RESPOND MSG="Filament Job    : {FILAMENT}"
  RESPOND MSG="----------- TOOLS -----------"

  {% for tool_num in printer.toolchanger.tool_numbers %}
    {% set ttemp = params['T' ~ tool_num|string ~ '_TEMP'] | default('-') %}
    {% set tfil = params['T' ~ tool_num|string ~ '_FILAMENT'] | default('-') %}
    {% set tpa = pa_map.get(tool_num|string, '-') %}
    {% set tr = retract_map.get(tool_num|string,{}) %}

    RESPOND MSG="T{tool_num} {tfil} | {ttemp}C | PA={tpa} | R={tr.get('length','-')}mm/{tr.get('speed','-')}mm/s"
  {% endfor %}

  RESPOND MSG="-----------------------------"


  # OPTIONAL FULL DEBUG TABLE
  {% if DEBUG %}
    RESPOND MSG="----------- DEBUG TOOLS -----------"

    {% for tool_num in printer.toolchanger.tool_numbers %}
      {% set ttemp = params['T' ~ tool_num|string ~ '_TEMP'] | default('-') %}
      {% set tfil = params['T' ~ tool_num|string ~ '_FILAMENT'] | default('-') %}
      {% set tpa = pa_map.get(tool_num|string, '-') %}
      {% set tr = retract_map.get(tool_num|string,{}) %}

      RESPOND MSG="T{tool_num}: {tfil} | {ttemp}C | PA={tpa} | R={tr.get('length','-')}/{tr.get('speed','-')}/{tr.get('unretract','-')}"
    {% endfor %}

    RESPOND MSG="----------------------------------"
    
  {% endif %}


  BED_MESH_CLEAR
  
  M117 Initializing...

  SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
  SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=False

  STOP_CRASH_DETECTION

  SET_GCODE_OFFSET Z=0

  M117 Settling to {BED_TEMP}C
  RESPOND MSG="Settling to {BED_TEMP}C"
  
  SET_LED LED=main_led WHITE=0.50 SYNC=0 TRANSMIT=1
  
  HEAT_SOAK BED={BED_TEMP} CHAMBER=0 SOAK_TIME=5 FILAMENT={FILAMENT}
  
  CHAMBER_RESET
  
  M117 Chamber Fan Start
  RESPOND MSG="Chamber Fan Start"
  CHAMBER_START FILAMENT={FILAMENT}
  
  M117 VOC Filter Start
  RESPOND MSG="VOC Filter Start"
  VOC_FILTER_START FILAMENT={FILAMENT}
  
  M117 Homing
  RESPOND MSG="Homing"
  G28
  G90
  
  M117 Quad Gantry Level
  RESPOND MSG="Quad Gantry Level"
  QUAD_GANTRY_LEVEL

  # {% if TOOL >= 0 %}
  #   M117 Selecting T{TOOL}
  #   RESPOND MSG="Selecting T{TOOL}"
  #   T{TOOL}
  # {% endif %}
  
  # M117 Home Z
  # RESPOND MSG="Home Z"
  # G28 Z


  # Always use T0 for Z homing
  M117 Selecting T0 for Z home
  RESPOND MSG="Selecting T0 for Z home"
  T0
  
  M117 Home Z
  RESPOND MSG="Home Z"
  G28 Z
  

  M117 Bed mesh
  RESPOND MSG="Bed mesh"
  BED_MESH_CALIBRATE METHOD=scan ADAPTIVE=1

  # Switch back to the requested print tool
  {% if TOOL >= 0 and TOOL != 0 %}
    M117 Selecting T{TOOL}
    RESPOND MSG="Selecting T{TOOL}"
    T{TOOL}
  {% endif %}
  
  SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=True
  SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=True

  M117 Parking nozzle
  RESPOND MSG="Parking nozzle"
  PARK_NOZZLE


  # FIX: safer tool apply block
  {% if TOOL >= 0 %}
    {% set r = retract_map.get(TOOL|string, {'length':0.5,'speed':30,'unretract':30}) %}
    {% set pa = pa_map.get(TOOL|string, 0.02) %}
    {% set tool_obj = printer["tool T" ~ TOOL|string] %}

    SET_RETRACTION RETRACT_LENGTH={r['length']} RETRACT_SPEED={r['speed']} UNRETRACT_EXTRA_LENGTH={tool_obj.params_unretract_extra_length} UNRETRACT_SPEED={r['unretract']}
    SET_PRESSURE_ADVANCE EXTRUDER={tool_obj.extruder} ADVANCE={pa}
  {% endif %}


  # FIX: safe temp selection
  {% if TOOL >= 0 %}
    {% set tparam = 'T' ~ TOOL|string ~ '_TEMP' %}
    {% set temp = params[tparam] | default(TOOL_TEMP, true) | int %}

    M117 Waiting on T{TOOL} {temp}C
    RESPOND MSG="Waiting on T{TOOL} {temp}C"
    M109 S{temp}

  {% else %}
    M109 S{TOOL_TEMP}
  {% endif %}


  START_CRASH_DETECTION

  M117 Start TDD polling
  TDD_POLLING_START
  TDD_START

  M117 Priming
  CLEAN_NOZZLE PURGE=50 WIPE=0

  G0 Z2 F300
  G92 E0

  M117 Printing
  RESPOND MSG="[PRINT_START] Complete - T{TOOL} filament={FILAMENT}"
















# ── Helper: move to absolute Z after tool unselect ─────────────────────────
[gcode_macro _POST_UNSELECT_SET_Z]
description: Move to absolute Z after tool unselect without exceeding max Z
gcode:
  {% set z_target = params.Z|default(10)|float %}
  {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}
  {% set final_z = [z_target, max_z]|min %}
  G90
  G0 Z{final_z} F1200




[gcode_macro PRINT_END]
description: Clean print end sequence for klipper-toolchanger-easy
gcode:

  {% set RETRACT_LENGTH = 2.0 %}
  {% set RETRACT_FEEDRATE = 3600 %}
  {% set Z_LIFT = 20 %}
  {% set PARK_X = 175 %}
  {% set PARK_Y = 345%}

  STOP_CRASH_DETECTION
  TDD_STOP
  TDD_POLLING_STOP

  CHAMBER_STOP
  CHAMBER_PURGE
  
  VOC_FILTER_POSTRUN_START

  M400
  SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
  SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=False

  {% set t = printer.toolchanger.tool_number|default(-1)|int %}

  {% if t >= 0 and printer["tool T" ~ t] is defined %}
    {% set extruder_name = printer["tool T" ~ t].extruder %}

    {% if printer[extruder_name].can_extrude %}
      RESPOND MSG="Retracting T{t}"
      G92 E0
      G1 E-{RETRACT_LENGTH} F{RETRACT_FEEDRATE}
      M400
    {% endif %}
  {% endif %}

  TURN_OFF_HEATERS

  {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}
  {% set current_z = printer.toolhead.position[2]|float %}
  {% set target_z = [current_z + Z_LIFT, max_z]|min %}

  G90
  G0 Z{target_z} F1200

  G0 X{PARK_X} Y{PARK_Y} F15000
  M400

  {% set current_tool = printer.toolchanger.tool_number|default(-1)|int %}
  {% if current_tool >= 0 %}
    UNSELECT_TOOL RESTORE_AXIS="XY"
    _POST_UNSELECT_SET_Z Z=10
  {% endif %}

  {% for tool_num in printer.toolchanger.tool_numbers %}
    {% set tool_obj = printer['tool T' ~ tool_num] %}
    {% if tool_obj.fan is defined and tool_obj.fan %}
      SET_FAN_SPEED FAN={tool_obj.fan} SPEED=0
    {% endif %}
  {% endfor %}

  BED_MESH_CLEAR
  M400

  {% set cfg = printer.configfile.config["printer"] %}
  SET_VELOCITY_LIMIT VELOCITY={cfg["max_velocity"]|float} ACCEL={cfg["max_accel"]|float} MINIMUM_CRUISE_RATIO={cfg["minimum_cruise_ratio"]|default(0.5)|float} SQUARE_CORNER_VELOCITY={cfg["square_corner_velocity"]|default(5.0)|float}

  M221 S100
  M220 S100
  M84
  SET_LED LED=main_led WHITE=0.25 SYNC=0 TRANSMIT=1
  M117 Print Complete



























# # -- Print State Tracker ----------------------------------------------------

# [gcode_macro _PRINT_TRACKER]
# description: Tracks printing state for toolchanger
# variable_is_printing: False
# gcode:
#   # This macro just holds the variable, no gcode needed


# [gcode_macro _SLICER_TOOL_PARAMS]
# variable_retract: {'0': {'length': 0.5, 'speed': 31, 'unretract': 31}, '1': {'length': 0.6, 'speed': 30, 'unretract': 30},  '2': {'length': 0.6, 'speed': 30, 'unretract': 30},'3': {'length': 0.6, 'speed': 30, 'unretract': 30}}
# gcode:
#   # Storage only


# [gcode_macro _SLICER_TOOL_PA]
# variable_pa: {'0': 0.02, '1': 0.02, '2': 0.02, '3': 0.02}
# gcode:
#   # Storage only


# # -- Print Start ------------------------------------------------------------

# [gcode_macro PRINT_START]
# description: Toolchanger print start - compliant with KTC-Easy and Cartographer touch docs
# variable_printing: False
# gcode:

#   # -- Parameters ------------------------------------------------------------
#   {% set TOOL = params.TOOL | default(-1) | int %}
#   {% set TOOL_TEMP = params.TOOL_TEMP | default(0) | int %}
#   {% set BED_TEMP = params.BED_TEMP | default(60) | int %}
#   {% set FILAMENT = params.FILAMENT | default("PLA") | upper %}

#   # -- Bed thermal control ---------------------------------------------------
#   {% set BED_SOAK = 0 %}
#   {% set PREHEAT_TEMP = BED_TEMP %}

#   # Clear saved temperatures
#   {% set empty_dict = {} %}
#   SET_GCODE_VARIABLE MACRO=SAVE_TOOL_TEMPS VARIABLE=saved_temps VALUE="{empty_dict}"
#   SET_GCODE_VARIABLE MACRO=SAVE_TOOL_TEMPS VARIABLE=saved_active_tool VALUE=-1

#   # -- Build slicer retraction map ------------------------------------------
#   {% set retract_map = {} %}
#   {% for tool_num in printer.toolchanger.tool_numbers %}
#     {% set rl = 'T' ~ tool_num|string ~ '_RETRACT_LEN' %}
#     {% set rs = 'T' ~ tool_num|string ~ '_RETRACT_SPEED' %}
#     {% set us = 'T' ~ tool_num|string ~ '_UNRETRACT_SPEED' %}
#     {% set tool_obj = printer['tool T' ~ tool_num|string] %}
#     {% set r_len = params[rl] | default(tool_obj.params_retract_length) | float %}
#     {% set r_spd = params[rs] | default(tool_obj.params_retract_speed) | float %}
#     {% set u_spd = params[us] | default(tool_obj.params_unretract_speed) | float %}
#     {% set _ = retract_map.update({tool_num|string: {'length': r_len, 'speed': r_spd, 'unretract': u_spd}}) %}
#   {% endfor %}

#   SET_GCODE_VARIABLE MACRO=_SLICER_TOOL_PARAMS VARIABLE=retract VALUE="{retract_map}"
#   RESPOND MSG="[PRINT_START] Raw params received: {params}"
#   RESPOND MSG="[PRINT_START] Retract map built: {retract_map}"

#   # -- Build slicer PA map ---------------------------------------------------
#   {% set pa_map = {} %}
#   {% for tool_num in printer.toolchanger.tool_numbers %}
#     {% set pa_param = 'T' ~ tool_num|string ~ '_PA' %}
#     {% set tool_obj = printer['tool T' ~ tool_num|string] %}
#     {% set extruder_name = tool_obj.extruder %}
#     {% set pa_default = printer[extruder_name].pressure_advance %}
#     {% set pa_val = params[pa_param] | default(pa_default) | float %}
#     {% set _ = pa_map.update({tool_num|string: pa_val}) %}
#   {% endfor %}

#   SET_GCODE_VARIABLE MACRO=_SLICER_TOOL_PA VARIABLE=pa VALUE="{pa_map}"
#   RESPOND MSG="[PRINT_START] PA map built: {pa_map}"

#   M117 Initializing...

#   SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
#   SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=False

#   STOP_CRASH_DETECTION

#   # -- Reset Z offset before homing ------------------------------------------
#   SET_GCODE_OFFSET Z=0

#   # -- Bed heat --------------------------------------------------------------
#   M117 Bed preheat {PREHEAT_TEMP}C
#   M140 S{PREHEAT_TEMP}
#   M190 S{PREHEAT_TEMP}

#   M117 Bed soak {BED_SOAK}m
#   G4 P{BED_SOAK * 60000}

#   M117 Settling to {BED_TEMP}C
#   M140 S{BED_TEMP}
#   M190 S{BED_TEMP}

#   # -- Chamber ---------------------------------------------------------------
#   M117 Chamber Fan Start
#   CHAMBER_FAN_START FILAMENT={FILAMENT}

#   M117 VOC Filter Start
#   VOC_FILTER_START FILAMENT={FILAMENT}
  
#   # -- Homing ---------------------------------------------------------------
#   M117 Homing
#   G28
#   G90

#   # -- Quad Gantry Level -----------------------------------------------------
#   M117 Quad Gantry Level
#   QUAD_GANTRY_LEVEL

#   # -- Tool select ----------------------------------------------------------
#   {% if TOOL >= 0 %}
#     M117 Selecting T{TOOL}
#     T{TOOL}
#   {% endif %}

#   # -- Z home ---------------------------------------------------------------
#   M117 Home Z
#   G28 Z

#   # -- Bed mesh -------------------------------------------------------------
#   M117 Bed mesh
#   BED_MESH_CALIBRATE METHOD=scan ADAPTIVE=1
#   #BED_MESH_PROFILE LOAD=default
#   # -- Mark printing active --------------------------------------------------
#   SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=True
#   SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=True

#   # -- Park nozzle ----------------------------------------------------------
#   M117 Parking nozzle
#   RESPOND MSG="[DEBUG] About to PARK_NOZZLE - current X={printer.toolhead.position.x} Y={printer.toolhead.position.y} Z={printer.toolhead.position.z}"
#   PARK_NOZZLE
#   RESPOND MSG="[DEBUG] PARK_NOZZLE completed"

#   # -- Apply slicer retract + PA --------------------------------------------
#   {% if TOOL >= 0 %}
#     {% set r = retract_map[TOOL|string] %}
#     {% set pa = pa_map[TOOL|string] %}
#     {% set tool_obj = printer["tool T" ~ TOOL|string] %}

#     RESPOND MSG="[PRINT_START] Applying T{TOOL} retract: length={r['length']} speed={r['speed']} unretract={r['unretract']}"
#     SET_RETRACTION RETRACT_LENGTH={r['length']} RETRACT_SPEED={r['speed']} UNRETRACT_EXTRA_LENGTH={tool_obj.params_unretract_extra_length} UNRETRACT_SPEED={r['unretract']}
#     GET_RETRACTION

#     RESPOND MSG="[PRINT_START] Applying T{TOOL} PA: {pa} on extruder {tool_obj.extruder}"
#     SET_PRESSURE_ADVANCE EXTRUDER={tool_obj.extruder} ADVANCE={pa}
#   {% endif %}

#   # -- Heat tool ------------------------------------------------------------
#   {% if TOOL >= 0 %}
#     {% set initialToolTempParam = 'T' ~ TOOL|string ~ '_TEMP' %}
#     {% set initialToolTemp = params[initialToolTempParam] | default(TOOL_TEMP) | int %}

#     M117 Waiting on T{TOOL} {initialToolTemp}C
#     M109 S{initialToolTemp}
#   {% else %}
#     M109 S{TOOL_TEMP}
#   {% endif %}

#   START_CRASH_DETECTION

#   # -- Tool detection --------------------------------------------------------
#   M117 Start TDD polling
#   TDD_POLLING_START
#   TDD_START

#   # -- Prime ---------------------------------------------------------------
#   M117 Priming
#   CLEAN_NOZZLE PURGE=50 WIPE=0

#   # -- Final prep ----------------------------------------------------------
#   G0 Z2 F300
#   G92 E0

#   M117 Printing
#   RESPOND MSG="[PRINT_START] Complete - T{TOOL} filament={FILAMENT}"
  
  
  



# # ── Helper: move to absolute Z after tool unselect ─────────────────────────
# [gcode_macro _POST_UNSELECT_SET_Z]
# description: Move to absolute Z after tool unselect without exceeding max Z
# gcode:
#   {% set z_target = params.Z|default(10)|float %}
#   {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}
#   {% set final_z = [z_target, max_z]|min %}
#   G90
#   G0 Z{final_z} F1200


# # ── Print End ──────────────────────────────────────────────────────────────
# [gcode_macro PRINT_END]
# description: Clean print end sequence for klipper-toolchanger-easy
# gcode:
#   # ── Configuration ────────────────────────────────────────────────────────
#   {% set RETRACT_LENGTH = 2.0 %}
#   {% set RETRACT_FEEDRATE = 3600 %}
#   {% set Z_LIFT = 20 %}
#   {% set PARK_X = 175 %}
#   {% set PARK_Y = 350 %}

#   # ── Stop detection and update state ──────────────────────────────────────
#   STOP_CRASH_DETECTION
#   TDD_STOP
#   TDD_POLLING_STOP
  
#   CHAMBER_FAN_STOP
#   VOC_FILTER_POSTRUN_START

#   M400
#   SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
#   SET_GCODE_VARIABLE MACRO=_PRINT_TRACKER VARIABLE=is_printing VALUE=False

# # ── Retract filament only if active tool is hot ─────────────────────────
#   {% set t = printer.toolchanger.tool_number|default(-1)|int %}
  
#     {% if t >= 0 %}
#       {% set extruder_name = printer["tool T" ~ t].extruder %}
  
#       {% if extruder_name %}
#         {% set current_temp = printer[extruder_name].temperature|float %}
  
#         {% if printer[extruder_name].can_extrude %}
#           RESPOND MSG="Retracting T{t} at {current_temp|round(1)}C"
#           G92 E0
#           G1 E-{RETRACT_LENGTH} F{RETRACT_FEEDRATE}
#           M400
#         {% else %}
#           RESPOND MSG="Skipping retract: T{t} cold ({current_temp|round(1)}C)"
#         {% endif %}
#       {% else %}
#         RESPOND MSG="Skipping retract: T{t} has no extruder"
#       {% endif %}
#     {% else %}
#       RESPOND MSG="Skipping retract: no active tool"
#     {% endif %}

#   # ── Turn off all heaters ─────────────────────────────────────────────────
#   TURN_OFF_HEATERS

#   # ── Lift Z to clear the print ────────────────────────────────────────────
#   {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}
#   {% set current_z = printer.toolhead.position[2]|float %}
#   {% set target_z = [current_z + Z_LIFT, max_z]|min %}
#   G90
#   G0 Z{target_z} F1200

#   # ── Move to park position ────────────────────────────────────────────────
#   G0 X{PARK_X} Y{PARK_Y} F15000
#   M400

#   # ── Park (dock) the active tool without selecting T0 ────────────────────
#   {% set current_tool = printer.toolchanger.tool_number|default(-1)|int %}
#   {% if current_tool >= 0 %}
#     RESPOND MSG="Parking T{current_tool}"
#     UNSELECT_TOOL RESTORE_AXIS="XY"
#     _POST_UNSELECT_SET_Z Z=10
#   {% endif %}

#   # ── Turn off all tool fans ───────────────────────────────────────────────
#   {% for tool_num in printer.toolchanger.tool_numbers %}
#     {% set tool_obj = printer['tool T' ~ tool_num] %}
#     {% if tool_obj.fan is defined and tool_obj.fan %}
#       SET_FAN_SPEED FAN={tool_obj.fan} SPEED=0
#     {% endif %}
#   {% endfor %}

#   # ── Clear bed mesh and finish up ─────────────────────────────────────────
#   BED_MESH_CLEAR
#   M400

#   # ── Reset motion limits ──────────────────────────────────────────────────
#   {% set cfg = printer.configfile.config["printer"] %}
#   {% set max_vel = cfg["max_velocity"]|float %}
#   {% set max_accel = cfg["max_accel"]|float %}
#   {% set mcr = cfg["minimum_cruise_ratio"]|default(0.5)|float %}
#   {% set scv = cfg["square_corner_velocity"]|default(5.0)|float %}
#   SET_VELOCITY_LIMIT VELOCITY={max_vel} ACCEL={max_accel} MINIMUM_CRUISE_RATIO={mcr} SQUARE_CORNER_VELOCITY={scv}

#   # ── Reset Flow / Speed ───────────────────────────────────────────────────
#   M221 S100
#   M220 S100
#   M84
#   M117 Print Complete






